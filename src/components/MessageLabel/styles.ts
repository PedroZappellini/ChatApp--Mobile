import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "70%",
    borderRadius: 10,
    marginVertical: 5,
    padding: 15,
  },
});

export default styles;
